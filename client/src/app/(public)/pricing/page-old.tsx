import PricingPage from "@/components/main/pricing";

export const metadata = {
  title: "Pricing - GenMeta Desktop",
  description: "Choose the right plan for GenMeta Desktop application",
};

export default function PricingRoute() {
  return <PricingPage />;
}
